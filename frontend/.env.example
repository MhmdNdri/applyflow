# Local dev can use /api/v1 through the Vite proxy.
# Deployed environments should use the full public API URL, for example:
# VITE_API_BASE_URL=https://applyflow-api-staging.onrender.com/api/v1
VITE_API_BASE_URL=/api/v1
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
